{
  "name": "app",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "vitest run",
    "test:ci": "vitest run --reporter=json --outputFile=.constat/check-report/raw.json"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "vitest": "^3.2.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
